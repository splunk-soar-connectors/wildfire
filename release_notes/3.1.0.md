* Added OAuth2 (Strata Cloud Manager) authentication using the client_credentials grant with automatic short-lived Bearer token acquisition and refresh [ESPM-5147]
* Added asset configuration parameters: **Authentication method**, **Client ID**, **Client Secret**, **TSG ID**, and **OAuth2 token URL**
* Retained legacy static API key authentication for backward compatibility; existing assets continue to work without reconfiguration
